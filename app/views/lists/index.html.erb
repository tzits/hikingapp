<div class='container'>
<% @lists.each do |l| %>
<div class="col-sm-4 col-lg-4"><h1><%= link_to l.name, category_path(l,l.categories.first)%></h1><br>
<%= image_tag l.image, class: 'col-sm-4 col-lg-4'%></div>

<% end %>
</div>
